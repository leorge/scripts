#!/bin/bash
for app in $*; do
    if ! which $app > /dev/null; then
        if which apt > /dev/null; then
            echo "Install $app..."
            sudo apt install -y $app
        fi
    fi
done
