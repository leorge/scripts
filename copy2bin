BIN=~/bin
if [ `pwd` = $BIN ]; then
    echo "Don't launch $0 in $BIN" >& 2
elif mkdir -p $BIN; then
    for cmd in *; do
        [ -x $cmd ] && cp $cmd $BIN/ # && echo $0: $cmd is copied. >&2
    done
	echo :$PATH: | grep :$HOME/bin: > /dev/null || export PATH=$PATH:$HOME/bin
fi
